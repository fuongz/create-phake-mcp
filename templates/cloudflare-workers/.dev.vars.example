# Cloudflare Workers MCP Server

# Generate encryption key: openssl rand -base64 32 | tr '+/' '-_' | tr -d '='
RS_TOKENS_ENC_KEY=

# Optional: OAuth client (if using OAuth)
# OAUTH_CLIENT_ID=
# OAUTH_CLIENT_SECRET=
# OAUTH_REDIRECT_URI=
# OAUTH_REDIRECT_ALLOWLIST=

# Optional: Auth strategy (oauth, google, github, bearer, api_key, custom, none)
# AUTH_STRATEGY=oauth
